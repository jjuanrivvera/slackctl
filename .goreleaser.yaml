# .goreleaser.yaml — copied into a generated CLI by cliwright (GOAL.md §5).
# Replace slackctl / jjuanrivvera / slackctl. Run `goreleaser check` in CI (not only at tag).
version: 2

before:
  hooks: [go mod tidy, ./scripts/completions.sh]

builds:
  - id: slackctl
    main: ./cmd/slackctl
    binary: slackctl
    env: [CGO_ENABLED=0]
    goos: [linux, darwin, windows]
    goarch: [amd64, arm64]
    ignore:
      - { goos: windows, goarch: arm64 }
    ldflags:
      - -s -w
      - -X {{ .ModulePath }}/internal/version.Version={{ .Version }}
      - -X {{ .ModulePath }}/internal/version.Commit={{ .Commit }}
      - -X {{ .ModulePath }}/internal/version.Date={{ .Date }}

archives:
  - formats: [tar.gz]
    format_overrides:
      - { goos: windows, formats: [zip] }
    files: [README.md, LICENSE, completions/*]

checksum: { name_template: checksums.txt }

# Docker: a tiny distroless image built from the prebuilt static amd64 binary.
# CI snapshot runs pass --skip=docker (no registry auth on PRs); the real image is built and
# pushed to GHCR at tag time (the release workflow logs in to ghcr.io first).
dockers:
  - id: slackctl
    ids: [slackctl]
    goos: linux
    goarch: amd64
    dockerfile: Dockerfile.goreleaser
    image_templates:
      - "ghcr.io/jjuanrivvera/slackctl:{{ .Version }}"
      - "ghcr.io/jjuanrivvera/slackctl:v{{ .Major }}"
      - "ghcr.io/jjuanrivvera/slackctl:latest"
    build_flag_templates:
      - "--label=org.opencontainers.image.title=slackctl"
      - "--label=org.opencontainers.image.description=A fast, scriptable CLI for the Slack Web API"
      - "--label=org.opencontainers.image.source=https://github.com/jjuanrivvera/slackctl"
      - "--label=org.opencontainers.image.version={{ .Version }}"
      - "--label=org.opencontainers.image.licenses=MIT"

# An explicit release block — without it, `goreleaser check` errors on a fresh repo
# ("scm releases: no remote configured to list refs from").
release:
  github: { owner: jjuanrivvera, name: slackctl }
  prerelease: auto

# Homebrew: use homebrew_casks (NOT the deprecated brews:). The cask is slackctl-cli,
# but the binary inside the archive is slackctl — so `binaries:` (plural list) is required,
# and an unsigned arm64 binary is SIGKILL'd by Gatekeeper without the xattr postflight.
homebrew_casks:
  - name: slackctl-cli
    binaries: [slackctl]
    # token required to push the cask cross-repo: the default GITHUB_TOKEN is scoped to this
    # repo only and cannot write to the tap.
    repository: { owner: jjuanrivvera, name: homebrew-slackctl, token: "{{ .Env.HOMEBREW_TAP_TOKEN }}" }
    homepage: https://github.com/jjuanrivvera/slackctl
    hooks:
      post:
        install: |
          system_command "/usr/bin/xattr", args: ["-dr", "com.apple.quarantine", "#{staged_path}/slackctl"]

scoops:
  - repository: { owner: jjuanrivvera, name: scoop-slackctl, token: "{{ .Env.HOMEBREW_TAP_TOKEN }}" }   # bucket repo must already exist; cross-repo token

nfpms:
  - formats: [deb, rpm, apk]
    bindir: /usr/bin
    maintainer: jjuanrivvera
    contents:
      - { src: completions/slackctl.bash, dst: /usr/share/bash-completion/completions/slackctl }
      - { src: completions/slackctl.zsh,  dst: /usr/share/zsh/site-functions/_slackctl }
      - { src: completions/slackctl.fish, dst: /usr/share/fish/vendor_completions.d/slackctl.fish }

sboms:
  - artifacts: archive          # syft SBOMs

signs:                          # cosign keyless (needs CI OIDC: permissions id-token: write)
  - cmd: cosign
    artifacts: checksum
    output: true
    args: ["sign-blob", "--yes", "--output-signature=${signature}", "--output-certificate=${certificate}", "${artifact}"]

changelog:
  groups:
    - { title: Features, regexp: '^.*?feat(\(.+\))??!?:.+$', order: 0 }
    - { title: Fixes, regexp: '^.*?fix(\(.+\))??!?:.+$', order: 1 }
    - { title: Performance, regexp: '^.*?perf(\(.+\))??!?:.+$', order: 2 }
  filters:
    exclude: ['^docs:', '^test:', '^chore:', '^ci:', merge conflict, Merge pull request, Merge branch]
